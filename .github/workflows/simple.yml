name: Shell Commands

# defining events that triggers the workflow
on: [push]

# defining jobs for this workflow
jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
      - name: echo a string
        run: echo "Hello World"
      - name: multiline script
        run: |
          node -v
          npm -v
